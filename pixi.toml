[workspace]
authors = ["Hong-yiii <hongyilin.mail@gmail.com>"]
channels = ["conda-forge"]
name = "graphrag"
platforms = ["osx-arm64"]
version = "0.1.0"

[tasks]

[dependencies]
python = "3.12.*"

[pypi-dependencies]
graphrag = ">=0.5.0"
requests = "*"
datasets = ">=2.14.0"
pandas = ">=1.5.0"
tqdm = ">=4.64.0"
python-dotenv = ">=0.19.0"
